from utility import Image
from utility import Collider

# spawn
# kill player if touching
# add point if (insert condition)
class Enemy(Image, Collider):
    def __init__(self, image_name, folder_name):
        pass
        # Image.__init(self)
        # Collider.__init__(self)

